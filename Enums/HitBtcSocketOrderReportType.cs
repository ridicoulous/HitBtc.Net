namespace HitBtc.Net.Enums
{
    /// <summary>
    /// Accepted values: status, new, canceled, expired, suspended, trade, replaced
    /// </summary>
    public enum HitBtcSocketOrderReportType
    {
        Status, 
        New, 
        Canceled, 
        Expired, 
        Suspended, 
        Trade,
        Replaced
    }
}